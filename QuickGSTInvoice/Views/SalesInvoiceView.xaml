<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:dxcv="clr-namespace:DevExpress.Maui.CollectionView;assembly=DevExpress.Maui.CollectionView"
             xmlns:local="clr-namespace:QuickGSTInvoice.ViewModels"
             xmlns:model="clr-namespace:QuickGSTInvoice.Models"
             xmlns:templates="clr-namespace:QuickGSTInvoice.Views"
             Title="{Binding Title}"
             IconImageSource="browse"
             BackgroundColor="{StaticResource NormalBackgroundColor}"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="true"
             x:Class="QuickGSTInvoice.Views.SalesInvoiceView">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="ADD" Command="{Binding AddItemCommand}" />
    </ContentPage.ToolbarItems>
    <Grid RowDefinitions="Auto, *">
        <StackLayout Margin="10">
            <Label x:Name="description" Text="This demo shows how to create simple invoice report using PDF grid with built-in styles." VerticalOptions="Center" TextColor="Black" Margin="0,0,5,0">
            </Label>
            <Label x:Name="label1" Text=" " VerticalOptions="Center"></Label>

            <Label x:Name="buttonDescription" Text="Please click the Generate PDF button to save and view the PDF document generated by the Syncfusion PDF library."
                  VerticalOptions="Center" TextColor="Black" Margin="0,0,5,0">
            </Label>

            <Label x:Name="label2" Text=" " VerticalOptions="Center"></Label>
            <Button x:Name="generateDocument" Clicked ="OnButtonClicked" Text ="Generate PDF" HorizontalOptions="Start"></Button>
            <Button  Clicked ="AddP_Clicked"  Text ="Generate PDF" HorizontalOptions="Start"></Button>

        </StackLayout>
        <!--<dxcv:DXCollectionView x:Name="ItemsListView" Grid.Row="2"
            ItemsSource="{Binding Items}"
                SelectionMode="None" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
            <dxcv:DXCollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <StackLayout Padding="18,20,10,01" x:DataType="model:Item">
                                <Label Text="{Binding Text}"
                                        LineBreakMode="NoWrap"
                                        Style="{DynamicResource ListItemTextStyle}"
                                        FontFamily="Roboto"
                                        FontSize="14"
                                        TextColor="{StaticResource NormalText}"/>
                                <Label Text="{Binding Description}"
                                        LineBreakMode="NoWrap"
                                        Style="{DynamicResource ListItemDetailTextStyle}"
                                        FontFamily="Roboto"
                                        FontSize="12"
                                        TextColor="{StaticResource NormalLightText}"/>
                                <StackLayout.GestureRecognizers>
                                    <TapGestureRecognizer
                                            NumberOfTapsRequired="1"
                                            Command="{Binding Source={RelativeSource AncestorType={x:Type local:ItemsViewModel}}, Path=ItemTapped}"
                                            CommandParameter="{Binding .}">
                                    </TapGestureRecognizer>
                                </StackLayout.GestureRecognizers>
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </dxcv:DXCollectionView.ItemTemplate>
            </dxcv:DXCollectionView>-->
    </Grid>
</ContentPage>